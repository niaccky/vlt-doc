{
  "name": "vlt-doc",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:build:pages": "node scripts/gh-pages.mjs build",
    "deploy": "node scripts/gh-pages.mjs deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vitepress": "^1.0.0"
  }
}
